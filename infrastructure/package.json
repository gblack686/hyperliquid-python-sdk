{
  "name": "infrastructure-openclaw",
  "version": "1.0.0",
  "description": "Secure AWS infrastructure for OpenClaw crypto trading - isolates signing key",
  "scripts": {
    "build": "tsc && cd lambda/trade-executor && npm run build",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "cdk deploy --all",
    "deploy:prod": "cdk deploy -c environment=prod",
    "diff": "cdk diff",
    "synth": "cdk synth"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "aws-cdk": "^2.120.0",
    "aws-cdk-lib": "^2.120.0",
    "constructs": "^10.3.0",
    "source-map-support": "^0.5.21",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.120.0",
    "constructs": "^10.3.0"
  }
}
