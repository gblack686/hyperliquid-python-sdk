# BTC Perps Order-Flow Mini-Kit (Free, Python)

This kit gives one runnable snippet per chart using public Binance Futures REST endpoints (no API key).
Run each script directly; images write to `out/` and snapshots to `data/orderflow.db`.

See each script's `--help` for options.
